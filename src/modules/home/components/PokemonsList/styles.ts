import { StyleSheet } from 'react-native';
import { COMMON_STYLES } from '../../../../common/styles';

const styles = StyleSheet.create({
  root: {},
  scrollContainer: {
    ...COMMON_STYLES.scrollInnerContainer,
    paddingHorizontal: 16,
  },
});

export default styles;
